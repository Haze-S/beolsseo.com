# beolsseo.com — 벌써아침 블로그 허브

`beolsseo.com` 루트 허브 페이지(정적, 단일 페이지). 주제별 블로그(dev.beolsseo.com 등)의 입구.

- 빌드 없음 — GitHub Pages 기본 정적 배포(`.nojekyll`). `index.html` + `assets/`.
- 블로그 추가: `assets/js/blogs.js` 의 `BLOGS` 배열에 한 줄 추가.
- 개인정보처리방침: `/privacy/`.
